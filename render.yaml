# Render Blueprint – Worker H24 pour les rappels FitMatch
# Voir README section "Déploiement sur Render (Worker rappels)"
services:
  - type: worker
    name: fitmatch-reminders-worker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python worker.py
    # Variables d'environnement : à configurer dans le dashboard Render (Environment)
    # Voir README pour la liste (DATABASE_URL, RESEND_API_KEY, SENDER_EMAIL, etc.)
